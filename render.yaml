services:
  - type: web
    name: aices-backend
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn backend.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: OPENAI_API_KEY
        sync: false
      - key: OPENAI_MODEL
        value: gpt-4o-mini
      - key: ALLOWED_ORIGINS
        value: https://chatbot-aices.vercel.app,http://localhost:5173,http://127.0.0.1:5173
      - key: AICES_DB_PATH
        value: /tmp/aices.db
    healthCheckPath: /health
